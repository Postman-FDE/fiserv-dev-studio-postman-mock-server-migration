# fiserv-dev-studio-postman-mock-server-migration
This repo contains the complete implementation specification for migrating Fiserv Dev Studio's mock server infrastructure from Prism to Postman Mock Servers. It is written to be self-contained — an engineer (or AI assistant) with access to Fiserv's codebase and this repo should have everything needed to implement the migration end-to-end.
